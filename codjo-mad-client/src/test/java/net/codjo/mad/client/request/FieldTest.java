package net.codjo.mad.client.request;

import static net.codjo.test.common.matcher.JUnitMatchers.*;
import org.junit.Test;

public class FieldTest {
    @Test
    public void test_mockTest() throws Exception {

    }

/*

    @Test
    public void test_equals() throws Exception {
        Field field = new Field("test", "fred");
        //noinspection ObjectEqualsNull
        assertThat(field.equals(null), equalTo(false));
        assertThat(field.equals(new Field(null, null)), equalTo(false));
        assertThat(field.equals(new Field("test", "olivier")), equalTo(false));
        assertThat(field.equals(new Field("test", "fred")), equalTo(true));
    }


    @Test
    public void test_hashCode() throws Exception {
        Field field = new Field("antipattern", "blob");
        assertThat(field.hashCode(), equalTo(new Field("antipattern", "blob").hashCode()));
    }
*/
}
